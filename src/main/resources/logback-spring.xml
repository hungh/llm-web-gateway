<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Application logging -->
    <logger name="com.prompt.security" level="INFO"/>
    
    <!-- Show ONNX classifier info -->
    <logger name="com.prompt.security.OnnxClassifier" level="INFO"/>
    
    <!-- Show BadActorClassifier debug for testing -->
    <logger name="com.prompt.security.BadActorClassifier" level="DEBUG"/>
    
    <!-- Spring Boot default logging -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache" level="WARN"/>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
